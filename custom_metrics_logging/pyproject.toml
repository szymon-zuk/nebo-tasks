[project]
name = "custom-metrics-logging"
version = "0.1.0"
description = "Custom metrics logging script for AWS EC2"
readme = "custom_metrics_logging.md"
requires-python = ">=3.11"
dependencies = [
    "black>=25.11.0",
    "isort>=7.0.0",
]
